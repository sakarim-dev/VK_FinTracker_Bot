"""Единое временное хранилище состояния пользователей.

Важно: все обработчики используют ОДИН экземпляр CtxStorage.
В исходной версии каждый handler создавал собственное хранилище,
из-за чего состояние могло быть недоступно другому обработчику.
"""
from vkbottle.tools import CtxStorage

ctx_storage = CtxStorage()
